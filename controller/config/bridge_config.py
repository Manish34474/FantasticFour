class BridgeConfig:

    """Class representing configuration parameters for a bridge in the simulation."""
    # total brides construction sites in simulation
    num_of_bridges = 4

    # initial completion of bridge construction
    initial_bridge_health = 0.8

    damage_rate = 0.1  # rate at which bridge health decreases when attacked
